# Bölüm B çalışma günlüğü

- 25.09.2026 11.03–11.04 Europe/Istanbul: Kullanıcı promptu ilk dosya yazımı olarak aynen `promptlar/B-n8n.md` içine kaydedildi; `case-brief.md` tamamen okundu. A başlangıç commit'i `c70a7ed`; A ve özgün girdiler korunacak. Plan: kaynak inceleme → bağlı workflow → gerçek Code/HTML ayarlarından çevrimdışı test → belgeleme ve yerel commit.
- Şablon sayfası web aracıyla açıldı. Web aracında şablon API'si erişilemez, raw kaynak cache miss döndü. PowerShell'in ilk HTTP denemeleri sandbox ağ kısıtına takıldı. İzinli tekrar ile şablon JSON'u, gerçek laptop HTML'i ve resmi n8n kaynakları indirildi; yalnızca Git dışında `.tmp/B-source/` içinde saklandı. n8n import/çalıştırma yapılmadı.
- Şablonun günlük Schedule, tekli döngü/bekleme, HTTP, HTML, normalizasyon, fiyat karşılaştırma, IF, Telegram ve Sheets düğümleri incelendi. Gerçek spreadsheet/chat/credential referansları yeni workflow'a kopyalanmıyor.
- Kaynak: `n8n@1.112.6`, commit `0c00d3b18c17ce36c62b54fecfa79215f649ebf8`. Üç tahmini kaynak dosya yolu 404 döndü; HTML/Telegram'ın tek dosyada, Sheets ortak açıklamalarının `sheet/commonDescription.ts` içinde olduğu doğrulanarak doğru kaynaklara geçildi.
- Kaynak HTML'de `.thumbnail` kartlar, `a.title[title]`, `.price [itemprop=price]`, `[itemprop=reviewCount]` ve `.pagination a[href]` incelendi. Sayfa bağlantıları aralıklı; maksimuma kadar ardışık planlama ve her sayfada yeni bağlantıları kontrol etme kararı alındı. Ürün kartları ayrı HTML parçaları olarak çıkarılacak; paralel alan dizileri farklı ürünleri birleştirmeyecek.
- HTML selector testleri için `cheerio@1.0.0-rc.12` mevcut A `node_modules` dizinine `--no-save --package-lock=false` ile kuruldu (15 paket, exit code 0). A manifesti/kilidi değişmedi; ikinci bağımlılık ağacı oluşturulmadı. Bu yalnızca test bağımlılığıdır, workflow Code düğümleri paket içe aktarmayacak.
- 11.12–11.24: Pasif, bağlı workflow oluşturuldu. HTTP ve HTML n8n düğümlerinde; Code yalnızca dönüşüm/doğrulama yapıyor. Seyrek sayfalama bağlantıları ardışık geziliyor; bütün tarama doğrulanmadan Sheets yazımı yok. Bildirim snapshot yazımından sonra, tamamlanma kaydından önce seçildi; başarısız bildirim sonraki çalışmanın referansını ilerletmiyor.
- n8n yürütme kaynağında `alwaysOutputData` ile ayrı hata çıkışının birlikte boş bir başarı item'ı üretebildiği görüldü. Sheets okuma düğümleri `continueRegularOutput` + hemen sonraki Code hata kontrolü kullanacak şekilde düzeltildi; gerçek boş sonuç ile okuma hatası ayrıldı. Kart sayısı kontrolü de son kartın kaybolmasını başarı saymamak için eklendi.
- İlk Vitest çalıştırması sandbox dışında üst dizin okunamadığı için başlamadı; izinli tekrar başarılı. İlk 15 test, bağlı grafikte başarı/hata senaryoları eklendikten sonra 17 test geçti. Yerel harness gerçek workflow Code kodlarını ve HTML selector'larını kullanıyor; HTTP/Sheets/Telegram işlemleri açıkça mock.
- Kaynak referansları için iki doküman URL'si 404 döndü; resmi sürüme sabitlenmiş kaynak kodu kullanıldı. Şablon JSON'u indirildi ve incelendi, n8n'e import edilmedi. Orijinal şablondaki kimlik/credential bilgilerini taşımamak için indirilen JSON teslim dosyalarına eklenmedi.
- 11.25–11.29: Resmi `n8n-nodes-base` bağımlılıklarıyla eşleşmesi için test ayrıştırıcıları `cheerio@1.0.0-rc.6` ve `html-to-text@9.0.5` olarak kuruldu (`--no-save --package-lock=false`; exit 0). rc.6 paketinin giriş yolu farklı olduğu için ilk yeniden çalıştırma modül bulunamadı hatası verdi; Vitest alias'ı `cheerio/index.js` olarak düzeltildi. **11.29.30: Vitest 3.2.7, 1 dosya / 17 test başarılı, 3.77 saniye, exit 0.**
- İndirilen gerçek ilk sayfa HTML'i, workflow Code kodları ve HTML selector'larıyla yerelde kontrol edildi: 6 kart, sayfalama bağlantılarından keşfedilen son sayfa 20, ilk ürün Packard 255 G2 / 416.99 / 2 yorum. Bunlar gözlem değerleridir, akışta sabit değildir. Bu kontrol bütün siteyi tarama veya canlı n8n koşumu değildir.
- 11.30.37: Yukarıdaki ilk sayfa kontrolü, tam eşleştirilen Cheerio/html-to-text sürümleriyle tekrar geçti (exit 0). `template.json` SHA-256 değeri açıklama belgesine kaydedildi; gerçek ID/credential içeren özgün indirme Git dışında tutuldu.
- Yerel commit'ler: `6cb3ab3` prompt/kaynak araştırması; `071a7ea` bağlı workflow ve 17 geçen test. Ardından `akis-aciklama.md` ve kök README hazırlandı; son dokümantasyon commit'i Git geçmişinden görülebilir. Push ve e-posta gönderilmedi.
- 11.35–11.36 son kontrolleri: JSON parse başarılı; 30 düğüm / 14 Code düğümü, `active: false`, Europe/Istanbul. `git diff --check` temiz. `git diff c70a7ed --exit-code -- A-mesaj-otomasyonu case-brief.md mesajlar.json promptlar/A-codex.md` değişiklik göstermedi. Özgün SHA-256 değerleri korundu: case-brief `AA6C90817B4552B9CA435C0155BCAC94F15295D9C23B13EE02D9B463EB1BA6FB`, mesajlar `E431D6EA36046F284FFA1DFC44904398423D30FAF6A90553684A534B738C553A`.
- Bölüm A test/typecheck/canlı koşumu bu aşamada yeniden çalıştırılmadı; önceki doğrulama kayıtları korundu. B için n8n import/runtime, bütün sitede canlı tarama, Google Sheets fiziksel yazımı ve Telegram teslimi yapılmadı. Credential'larla gerçek kullanım kontrolü ve aynı tablo için tek execution şartı belgede açıklandı.
